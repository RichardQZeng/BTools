{% set name = "BERATools" %}
{% set version = "0.2.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/RichardQZeng/BTools/archive/{{ version }}.tar.gz
  sha256: a58f593d4b33d46e198e5a22aeb07a7fac4ea28f92731d1b799ee44ebbc3b75b



build:
  entry_points:
    - BERATools = beratools.gui:gui_main
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python >=3.9
    - hatchling
    - pip
  run:
    - python >=3.9
    - dask
    - bera_centerlines
    - geopandas
    - pip
    - pyogrio
    - psutil
    - pyqt
    - rioxarray
    - rpy2
    - r-essentials
    - r-lidr
    - r-rcsf
    - r-rlas
    - r-sf
    - r-sp
    - r-terra
    - scikit-image
    - xarray-spatial

test:
  imports:
    - beratools
  commands:
    - pip check
    - BERATools --help
  requires:
    - pip

about:
  home: https://github.com/RichardQZeng/BTools.git
  summary: An advanced forest line feature analysis platform
  license: MIT
  license_file:
    - LICENSE

extra:
  recipe-maintainers:
    - richard
